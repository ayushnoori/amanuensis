import Edit${singularPascalName}Cell from '${importComponentEditNameCell}';

type ${pascalName}PageProps = {
  id: ${idTsType};
};

export default function Edit${singularPascalName}Page({ id }: ${pascalName}PageProps) {
  return <Edit${singularPascalName}Cell id={id} />;
}
