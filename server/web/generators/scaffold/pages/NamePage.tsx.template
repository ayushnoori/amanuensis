import ${singularPascalName}Cell from '${importComponentNameCell}';

type ${pascalName}PageProps = {
  id: ${idTsType};
};

export default function ${singularPascalName}Page({ id }: ${pascalName}PageProps) {
  return <${singularPascalName}Cell id={id} />;
}
